import { DashboardShell } from "@/components/dashboard/dashboard-shell";

export const metadata = { title: "My Popup Kits — Popdock" };

export default function DashboardPage() {
  return <DashboardShell />;
}
